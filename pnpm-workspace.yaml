# Monorepo: the Next.js app is the workspace root, the engine lives in
# packages/core and is consumed as @devnolife/karirku-core.
packages:
  - "packages/*"

ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
