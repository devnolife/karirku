export const SITE = {
  name: "CraftWorks",
  tagline: "Craft your career, and it works.",
  email: "halo@craftworks.id",
  /**
   * Social profiles. Set `href` to the real profile URL to activate the link —
   * entries left as `null` render as non-interactive "segera hadir" items
   * instead of dead `#` links.
   */
  social: [
    { label: "Instagram", href: null as string | null },
    { label: "LinkedIn", href: null as string | null },
    { label: "X / Twitter", href: null as string | null },
  ],
} as const;
