# @devnolife packages are published to GitHub Packages. Only needed when
# publishing packages/core, or when another repo consumes it (read:packages):
#   npm config set //npm.pkg.github.com/:_authToken <TOKEN>
@devnolife:registry=https://npm.pkg.github.com
